package com.example.myapplication.stuff

import androidx.appcompat.app.AppCompatActivity

class AndroidNavigator(private val activity: AppCompatActivity): Navigator {
    override fun showAnotherScreen() {
        // TODO
    }
}